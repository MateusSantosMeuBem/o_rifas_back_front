export interface ContactButtonProps {
    /* Message with numbers and pix. */
    message: string,
    /* Name of the seller. */
    sellerName: string,
    /* Contact of the seller. */
    contact: string,
}